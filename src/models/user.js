export default class User {
    constructor(username, email, password, usercode) {
        
      this.username = username;
      this.email = email;
      this.password = password;
        this.usercode = usercode;
    }
  }