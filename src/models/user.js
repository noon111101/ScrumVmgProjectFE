export default class User {

  
    constructor(username, email, password, fullname, department,code, gender,role) {
      this.username = username;
      this.email = email;
      this.password = password;
      this.fullname = fullname;
      this.department = department;
      this.code = code;
      this.gender = gender;
      this.role = role;
    }
  }